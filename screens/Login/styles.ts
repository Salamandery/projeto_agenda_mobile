import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: "center",
    paddingTop: 120,
  },
  Input: {
    fontSize: 20,
    borderWidth: 1,
    borderColor: "#3bc7f5",
    borderRadius: 10,
    marginBottom: 20,
    padding: 5,
  },
});
